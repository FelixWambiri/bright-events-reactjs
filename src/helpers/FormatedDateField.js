import React from "react";
import moment from "moment";
// {classes,input,type,value,placeholder,meta:{touched,dirty,error}}
const FormatedDateField = (props)=> {

    console.log("the props are now ", props)
    // const dataObject = new Date(value)
    // const momentDate = moment(dataObject)
    // const formatedDate = momentDate.format('dd-mm-yyyy')
    // return (
    //     <div className={`field ${touched?error?'error':'':''}`}>
    //         <input {...input} type="date" value={formatedDate} />
    //         {touched &&
    //         ((error && <span className={classes.errorMessage}>{error}</span>))}
    //     </div>
    // )
    return <h6>Hi</h6>
}
export default FormatedDateField